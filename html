<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>COPA STORE — Álbuns & Figurinhas</title>
<link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Barlow:ital,wght@0,300;0,400;1,300&display=swap" rel="stylesheet">
<style>
  :root {
    --gold: #C9A84C;
    --off-white: #F5F2EC;
    --dark: #0A0A0A;
    --mid: #1A1A1A;
    --text-muted: #888;
    --accent: #2D6A4F;
  }

  *, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }

  html { scroll-behavior: smooth; }

  body {
    background: var(--dark);
    color: var(--off-white);
    font-family: 'Barlow', sans-serif;
    font-weight: 300;
    overflow-x: hidden;
    cursor: none;
  }

  /* Custom cursor */
  .cursor {
    width: 10px; height: 10px;
    background: var(--gold);
    border-radius: 50%;
    position: fixed;
    pointer-events: none;
    z-index: 9999;
    transform: translate(-50%, -50%);
    transition: transform 0.1s ease, width 0.3s ease, height 0.3s ease, background 0.3s;
  }
  .cursor-ring {
    width: 36px; height: 36px;
    border: 1px solid rgba(201,168,76,0.4);
    border-radius: 50%;
    position: fixed;
    pointer-events: none;
    z-index: 9998;
    transform: translate(-50%, -50%);
    transition: left 0.12s ease, top 0.12s ease, width 0.3s ease, height 0.3s ease;
  }
  body:hover .cursor { opacity: 1; }

  /* NAV */
  nav {
    position: fixed; top: 0; left: 0; right: 0;
    z-index: 100;
    display: flex; align-items: center; justify-content: space-between;
    padding: 28px 5vw;
    mix-blend-mode: normal;
  }
  nav::before {
    content: '';
    position: absolute; inset: 0;
    background: linear-gradient(to bottom, rgba(10,10,10,0.95), transparent);
    pointer-events: none;
  }
  .nav-logo {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 1.6rem;
    letter-spacing: 0.1em;
    color: var(--gold);
    position: relative;
    z-index: 1;
    text-decoration: none;
  }
  .nav-links {
    display: flex; gap: 40px; list-style: none;
    position: relative; z-index: 1;
  }
  .nav-links a {
    color: var(--off-white);
    text-decoration: none;
    font-size: 0.78rem;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    opacity: 0.7;
    transition: opacity 0.3s;
  }
  .nav-links a:hover { opacity: 1; }
  .nav-cart {
    position: relative; z-index: 1;
    background: none; border: 1px solid rgba(201,168,76,0.5);
    color: var(--gold); padding: 10px 22px;
    font-family: 'Barlow', sans-serif;
    font-size: 0.75rem; letter-spacing: 0.2em; text-transform: uppercase;
    cursor: none; transition: all 0.3s;
  }
  .nav-cart:hover { background: var(--gold); color: var(--dark); }

  /* HERO */
  .hero {
    height: 100vh;
    display: flex; align-items: center;
    padding: 0 5vw;
    position: relative;
    overflow: hidden;
  }
  .hero-bg {
    position: absolute; inset: 0;
    background: radial-gradient(ellipse at 70% 50%, rgba(201,168,76,0.07) 0%, transparent 60%),
                radial-gradient(ellipse at 20% 80%, rgba(45,106,79,0.08) 0%, transparent 50%);
  }
  .hero-number {
    position: absolute;
    right: 5vw; top: 50%;
    transform: translateY(-50%);
    font-family: 'Bebas Neue', sans-serif;
    font-size: clamp(180px, 22vw, 340px);
    color: transparent;
    -webkit-text-stroke: 1px rgba(201,168,76,0.12);
    letter-spacing: -0.02em;
    line-height: 1;
    user-select: none;
    animation: floatNum 8s ease-in-out infinite;
  }
  @keyframes floatNum {
    0%, 100% { transform: translateY(-50%) translateX(0); }
    50% { transform: translateY(-52%) translateX(-10px); }
  }
  .hero-content { position: relative; z-index: 1; }
  .hero-tag {
    display: inline-block;
    font-size: 0.72rem; letter-spacing: 0.3em; text-transform: uppercase;
    color: var(--gold); margin-bottom: 24px;
    opacity: 0; animation: fadeUp 0.8s 0.3s forwards;
  }
  .hero-title {
    font-family: 'Bebas Neue', sans-serif;
    font-size: clamp(72px, 10vw, 160px);
    line-height: 0.92;
    letter-spacing: 0.01em;
    color: var(--off-white);
    opacity: 0; animation: fadeUp 0.8s 0.5s forwards;
  }
  .hero-title span { color: var(--gold); }
  .hero-sub {
    margin-top: 28px;
    font-size: 1rem;
    color: var(--text-muted);
    font-style: italic;
    letter-spacing: 0.05em;
    max-width: 380px;
    line-height: 1.7;
    opacity: 0; animation: fadeUp 0.8s 0.7s forwards;
  }
  .hero-cta {
    margin-top: 48px;
    display: flex; gap: 16px; align-items: center;
    opacity: 0; animation: fadeUp 0.8s 0.9s forwards;
  }
  .btn-primary {
    background: var(--gold); color: var(--dark);
    border: none; padding: 16px 40px;
    font-family: 'Barlow', sans-serif;
    font-size: 0.78rem; font-weight: 400;
    letter-spacing: 0.25em; text-transform: uppercase;
    cursor: none; transition: all 0.3s;
  }
  .btn-primary:hover { background: var(--off-white); transform: translateY(-2px); }
  .btn-ghost {
    background: none; border: none;
    color: var(--text-muted);
    font-size: 0.78rem; letter-spacing: 0.15em; text-transform: uppercase;
    cursor: none; transition: color 0.3s;
    display: flex; align-items: center; gap: 10px;
  }
  .btn-ghost::after {
    content: '→';
    transition: transform 0.3s;
  }
  .btn-ghost:hover { color: var(--off-white); }
  .btn-ghost:hover::after { transform: translateX(4px); }

  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(24px); }
    to { opacity: 1; transform: translateY(0); }
  }

  /* TICKER */
  .ticker {
    border-top: 1px solid rgba(255,255,255,0.06);
    border-bottom: 1px solid rgba(255,255,255,0.06);
    overflow: hidden; padding: 14px 0;
    background: rgba(201,168,76,0.04);
  }
  .ticker-track {
    display: flex; gap: 60px;
    animation: ticker 20s linear infinite;
    white-space: nowrap;
  }
  .ticker-track span {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 0.95rem; letter-spacing: 0.25em;
    color: var(--gold); opacity: 0.5;
  }
  .ticker-track span.dot {
    opacity: 0.2; font-size: 0.5rem;
    display: inline-flex; align-items: center;
  }
  @keyframes ticker {
    from { transform: translateX(0); }
    to { transform: translateX(-50%); }
  }

  /* SECTION BASE */
  section { padding: 120px 5vw; }
  .section-label {
    font-size: 0.7rem; letter-spacing: 0.35em; text-transform: uppercase;
    color: var(--gold); margin-bottom: 16px; opacity: 0.8;
  }
  .section-title {
    font-family: 'Bebas Neue', sans-serif;
    font-size: clamp(42px, 5vw, 72px);
    letter-spacing: 0.02em;
    line-height: 1;
    color: var(--off-white);
    margin-bottom: 60px;
  }

  /* FEATURED PRODUCT */
  .featured {
    display: grid; grid-template-columns: 1fr 1fr;
    gap: 80px; align-items: center;
  }
  .featured-visual {
    position: relative;
    aspect-ratio: 4/5;
    background: var(--mid);
    overflow: hidden;
    display: flex; align-items: center; justify-content: center;
  }
  .featured-visual::before {
    content: '';
    position: absolute; inset: 0;
    background: radial-gradient(circle at 50% 50%, rgba(201,168,76,0.15) 0%, transparent 70%);
  }
  .album-mockup {
    width: 65%;
    position: relative; z-index: 1;
    transition: transform 0.6s cubic-bezier(0.23, 1, 0.32, 1);
  }
  .featured-visual:hover .album-mockup { transform: scale(1.04) rotate(-1deg); }
  .album-svg { width: 100%; }
  .featured-badge {
    position: absolute; top: 24px; right: 24px;
    background: var(--gold); color: var(--dark);
    font-family: 'Bebas Neue', sans-serif;
    font-size: 1.2rem; letter-spacing: 0.05em;
    padding: 10px 16px;
  }
  .featured-info .price {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 3rem; color: var(--gold); letter-spacing: 0.02em;
  }
  .featured-info .price-sub {
    font-size: 0.78rem; color: var(--text-muted);
    letter-spacing: 0.1em; margin-bottom: 40px;
  }
  .featured-desc {
    font-size: 0.95rem; color: var(--text-muted);
    line-height: 1.9; margin-bottom: 40px; max-width: 420px;
  }
  .specs {
    display: grid; grid-template-columns: 1fr 1fr;
    gap: 20px; margin-bottom: 48px;
  }
  .spec { border-top: 1px solid rgba(255,255,255,0.07); padding-top: 16px; }
  .spec-label { font-size: 0.68rem; letter-spacing: 0.2em; text-transform: uppercase; color: var(--text-muted); margin-bottom: 4px; }
  .spec-val { font-size: 0.9rem; color: var(--off-white); }

  /* PRODUCTS GRID */
  .products-grid {
    display: grid; grid-template-columns: repeat(3, 1fr);
    gap: 2px;
  }
  .product-card {
    background: var(--mid);
    padding: 48px 32px 36px;
    position: relative; overflow: hidden;
    transition: background 0.4s;
    cursor: none;
    display: flex; flex-direction: column;
  }
  .product-card:hover { background: #202020; }
  .product-card::after {
    content: '';
    position: absolute; bottom: 0; left: 0; right: 0; height: 2px;
    background: var(--gold);
    transform: scaleX(0); transform-origin: left;
    transition: transform 0.4s cubic-bezier(0.23, 1, 0.32, 1);
  }
  .product-card:hover::after { transform: scaleX(1); }
  .product-icon {
    font-size: 3.5rem; margin-bottom: 32px;
    display: block;
    transition: transform 0.4s;
  }
  .product-card:hover .product-icon { transform: scale(1.1); }
  .product-name {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 1.8rem; letter-spacing: 0.04em;
    color: var(--off-white); margin-bottom: 10px;
  }
  .product-desc {
    font-size: 0.82rem; color: var(--text-muted);
    line-height: 1.8; flex: 1; margin-bottom: 28px;
  }
  .product-price {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 1.5rem; color: var(--gold); letter-spacing: 0.03em;
    display: flex; align-items: baseline; gap: 6px;
  }
  .product-price small { font-size: 0.7rem; color: var(--text-muted); font-family: 'Barlow', sans-serif; letter-spacing: 0.1em; }

  /* COUNTER / STATS */
  .stats {
    display: grid; grid-template-columns: repeat(4, 1fr);
    gap: 2px; margin-top: 0;
    background: var(--mid);
  }
  .stat-item {
    padding: 48px 32px; background: var(--mid);
    border-right: 1px solid rgba(255,255,255,0.05);
    text-align: center;
  }
  .stat-item:last-child { border-right: none; }
  .stat-num {
    font-family: 'Bebas Neue', sans-serif;
    font-size: clamp(3rem, 5vw, 5rem);
    color: var(--gold); letter-spacing: 0.02em; line-height: 1;
  }
  .stat-label {
    font-size: 0.72rem; letter-spacing: 0.25em; text-transform: uppercase;
    color: var(--text-muted); margin-top: 8px;
  }

  /* PACKS SECTION */
  .packs {
    background: var(--mid);
  }
  .packs-grid {
    display: grid; grid-template-columns: 2fr 1fr 1fr;
    gap: 2px;
  }
  .pack-card {
    padding: 56px 40px;
    background: var(--dark);
    position: relative; overflow: hidden;
    cursor: none; transition: background 0.4s;
  }
  .pack-card:hover { background: #111; }
  .pack-card.highlight {
    background: var(--gold);
  }
  .pack-card.highlight .pack-name,
  .pack-card.highlight .pack-price { color: var(--dark); }
  .pack-card.highlight .pack-desc { color: rgba(10,10,10,0.65); }
  .pack-label {
    font-size: 0.68rem; letter-spacing: 0.3em; text-transform: uppercase;
    color: var(--gold); margin-bottom: 32px;
  }
  .pack-card.highlight .pack-label { color: var(--dark); opacity: 0.6; }
  .pack-name {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 2.6rem; letter-spacing: 0.03em;
    color: var(--off-white); line-height: 1; margin-bottom: 16px;
  }
  .pack-desc {
    font-size: 0.82rem; color: var(--text-muted);
    line-height: 1.9; margin-bottom: 40px;
  }
  .pack-price {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 3.5rem; color: var(--gold); letter-spacing: 0.02em;
  }
  .pack-buy {
    margin-top: 32px;
    background: none; border: 1px solid rgba(255,255,255,0.15);
    color: var(--off-white); padding: 14px 28px;
    font-family: 'Barlow', sans-serif;
    font-size: 0.75rem; letter-spacing: 0.2em; text-transform: uppercase;
    cursor: none; transition: all 0.3s; display: inline-block;
    width: 100%; text-align: center;
  }
  .pack-card.highlight .pack-buy {
    border-color: rgba(10,10,10,0.25); color: var(--dark);
  }
  .pack-buy:hover { background: var(--off-white); color: var(--dark); border-color: transparent; }

  /* NEWSLETTER */
  .newsletter {
    text-align: center; padding: 100px 5vw;
    position: relative; overflow: hidden;
  }
  .newsletter::before {
    content: 'COPA';
    position: absolute; top: 50%; left: 50%;
    transform: translate(-50%, -50%);
    font-family: 'Bebas Neue', sans-serif;
    font-size: clamp(150px, 22vw, 360px);
    color: transparent;
    -webkit-text-stroke: 1px rgba(201,168,76,0.06);
    letter-spacing: 0.1em;
    pointer-events: none; white-space: nowrap;
  }
  .newsletter .section-title { margin-bottom: 16px; }
  .newsletter p {
    color: var(--text-muted); font-size: 0.9rem; margin-bottom: 40px;
    position: relative; z-index: 1;
  }
  .newsletter-form {
    display: flex; gap: 0; max-width: 480px;
    margin: 0 auto; position: relative; z-index: 1;
  }
  .newsletter-form input {
    flex: 1; background: var(--mid);
    border: 1px solid rgba(255,255,255,0.08);
    border-right: none;
    color: var(--off-white); padding: 16px 20px;
    font-family: 'Barlow', sans-serif;
    font-size: 0.85rem;
    outline: none;
  }
  .newsletter-form input::placeholder { color: var(--text-muted); }
  .newsletter-form button {
    background: var(--gold); border: none;
    color: var(--dark); padding: 16px 28px;
    font-family: 'Barlow', sans-serif;
    font-size: 0.75rem; letter-spacing: 0.2em; text-transform: uppercase;
    cursor: none; transition: background 0.3s;
    white-space: nowrap;
  }
  .newsletter-form button:hover { background: var(--off-white); }

  /* FOOTER */
  footer {
    border-top: 1px solid rgba(255,255,255,0.06);
    padding: 60px 5vw;
    display: flex; justify-content: space-between; align-items: center;
  }
  .footer-logo {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 1.4rem; letter-spacing: 0.1em; color: var(--gold);
    text-decoration: none;
  }
  .footer-links {
    display: flex; gap: 32px; list-style: none;
  }
  .footer-links a {
    color: var(--text-muted); text-decoration: none;
    font-size: 0.75rem; letter-spacing: 0.15em; text-transform: uppercase;
    transition: color 0.3s;
  }
  .footer-links a:hover { color: var(--off-white); }
  .footer-copy { font-size: 0.72rem; color: var(--text-muted); opacity: 0.5; }

  /* SCROLL REVEAL */
  .reveal {
    opacity: 0; transform: translateY(32px);
    transition: opacity 0.8s cubic-bezier(0.23, 1, 0.32, 1),
                transform 0.8s cubic-bezier(0.23, 1, 0.32, 1);
  }
  .reveal.visible { opacity: 1; transform: translateY(0); }

  @media (max-width: 900px) {
    .featured { grid-template-columns: 1fr; }
    .products-grid { grid-template-columns: 1fr; }
    .packs-grid { grid-template-columns: 1fr; }
    .stats { grid-template-columns: 1fr 1fr; }
    nav .nav-links { display: none; }
    footer { flex-direction: column; gap: 28px; text-align: center; }
    .footer-links { flex-wrap: wrap; justify-content: center; }
  }
</style>
</head>
<body>

<div class="cursor" id="cursor"></div>
<div class="cursor-ring" id="cursorRing"></div>

<!-- NAV -->
<nav>
  <a href="#" class="nav-logo">COPA STORE</a>
  <ul class="nav-links">
    <li><a href="#produtos">Produtos</a></li>
    <li><a href="#pacotes">Pacotes</a></li>
    <li><a href="#sobre">Sobre</a></li>
  </ul>
  <button class="nav-cart">🛒 Carrinho (0)</button>
</nav>

<!-- HERO -->
<section class="hero">
  <div class="hero-bg"></div>
  <div class="hero-number">2026</div>
  <div class="hero-content">
    <div class="hero-tag">Coleção Oficial · Copa do Mundo 2026</div>
    <h1 class="hero-title">
      COMPLETE<br>
      SEU <span>ÁLBUM</span>.
    </h1>
    <p class="hero-sub">Figurinhas, álbuns e colecionáveis para o maior evento do futebol mundial.</p>
    <div class="hero-cta">
      <button class="btn-primary">Ver Coleção</button>
      <button class="btn-ghost">Saiba mais</button>
    </div>
  </div>
</section>

<!-- TICKER -->
<div class="ticker">
  <div class="ticker-track" id="tickerTrack">
    <span>BRASIL</span><span class="dot">◆</span>
    <span>ARGENTINA</span><span class="dot">◆</span>
    <span>PORTUGAL</span><span class="dot">◆</span>
    <span>ESPANHA</span><span class="dot">◆</span>
    <span>ALEMANHA</span><span class="dot">◆</span>
    <span>FRANCA</span><span class="dot">◆</span>
    <span>ITALIA</span><span class="dot">◆</span>
    <span>HOLANDA</span><span class="dot">◆</span>
    <span>INGLETERRA</span><span class="dot">◆</span>
    <span>COPA 2026</span><span class="dot">◆</span>
    <span>BRASIL</span><span class="dot">◆</span>
    <span>ARGENTINA</span><span class="dot">◆</span>
    <span>PORTUGAL</span><span class="dot">◆</span>
    <span>ESPANHA</span><span class="dot">◆</span>
    <span>ALEMANHA</span><span class="dot">◆</span>
    <span>FRANCA</span><span class="dot">◆</span>
    <span>ITALIA</span><span class="dot">◆</span>
    <span>HOLANDA</span><span class="dot">◆</span>
    <span>INGLETERRA</span><span class="dot">◆</span>
    <span>COPA 2026</span><span class="dot">◆</span>
  </div>
</div>

<!-- FEATURED -->
<section id="produtos">
  <div class="reveal">
    <div class="section-label">Destaque</div>
    <h2 class="section-title">ÁLBUM OFICIAL</h2>
  </div>
  <div class="featured reveal">
    <div class="featured-visual">
      <div class="album-mockup">
        <svg class="album-svg" viewBox="0 0 280 380" fill="none" xmlns="http://www.w3.org/2000/svg">
          <rect width="280" height="380" rx="4" fill="#111"/>
          <rect x="1" y="1" width="278" height="378" rx="3" stroke="#C9A84C" stroke-width="0.5" stroke-opacity="0.4"/>
          <!-- Cover design -->
          <rect x="20" y="20" width="240" height="200" fill="#1A1A1A"/>
          <!-- Stadium arc -->
          <path d="M20 220 Q140 60 260 220" stroke="#C9A84C" stroke-width="1.5" fill="none" stroke-opacity="0.7"/>
          <path d="M40 220 Q140 90 240 220" stroke="#C9A84C" stroke-width="0.8" fill="none" stroke-opacity="0.3"/>
          <!-- Trophy icon -->
          <g transform="translate(110, 100)">
            <path d="M30 0 L50 0 L50 30 Q50 50 30 55 Q10 50 10 30 L10 0 Z" fill="none" stroke="#C9A84C" stroke-width="1.5"/>
            <path d="M10 10 Q0 10 0 25 Q0 38 10 40" stroke="#C9A84C" stroke-width="1.2" fill="none"/>
            <path d="M50 10 Q60 10 60 25 Q60 38 50 40" stroke="#C9A84C" stroke-width="1.2" fill="none"/>
            <line x1="30" y1="55" x2="30" y2="68" stroke="#C9A84C" stroke-width="1.5"/>
            <rect x="18" y="68" width="24" height="4" fill="#C9A84C" opacity="0.7"/>
          </g>
          <!-- Title area -->
          <rect x="20" y="236" width="240" height="64" fill="#0A0A0A"/>
          <text x="140" y="262" font-family="Georgia, serif" font-size="9" fill="#C9A84C" text-anchor="middle" letter-spacing="4">COPA DO MUNDO</text>
          <text x="140" y="285" font-family="Georgia, serif" font-size="22" fill="#F5F2EC" text-anchor="middle" letter-spacing="3" font-weight="bold">2026</text>
          <!-- Sticker placeholders -->
          <g opacity="0.4">
            <rect x="20" y="312" width="34" height="34" rx="2" fill="#1A1A1A" stroke="#C9A84C" stroke-width="0.5"/>
            <rect x="60" y="312" width="34" height="34" rx="2" fill="#1A1A1A" stroke="#C9A84C" stroke-width="0.5"/>
            <rect x="100" y="312" width="34" height="34" rx="2" fill="#1A1A1A" stroke="#C9A84C" stroke-width="0.5"/>
            <rect x="140" y="312" width="34" height="34" rx="2" fill="#C9A84C" opacity="0.3"/>
            <rect x="180" y="312" width="34" height="34" rx="2" fill="#1A1A1A" stroke="#C9A84C" stroke-width="0.5"/>
            <rect x="220" y="312" width="40" height="34" rx="2" fill="#1A1A1A" stroke="#C9A84C" stroke-width="0.5"/>
          </g>
        </svg>
      </div>
      <div class="featured-badge">NOVO</div>
    </div>
    <div class="featured-info">
      <div class="price">R$ 29,90</div>
      <div class="price-sub">frete grátis acima de R$ 150</div>
      <p class="featured-desc">
        O álbum oficial da Copa do Mundo 2026. 640 figurinhas para colecionar, 48 seleções, jogadores históricos e muito mais. A coleção mais completa já feita.
      </p>
      <div class="specs">
        <div class="spec">
          <div class="spec-label">Figurinhas</div>
          <div class="spec-val">640 total</div>
        </div>
        <div class="spec">
          <div class="spec-label">Seleções</div>
          <div class="spec-val">48 países</div>
        </div>
        <div class="spec">
          <div class="spec-label">Páginas</div>
          <div class="spec-val">100 páginas</div>
        </div>
        <div class="spec">
          <div class="spec-label">Edição</div>
          <div class="spec-val">Oficial FIFA</div>
        </div>
      </div>
      <button class="btn-primary">Adicionar ao Carrinho</button>
    </div>
  </div>
</section>

<!-- STATS -->
<div class="stats reveal">
  <div class="stat-item">
    <div class="stat-num" data-target="12450">0</div>
    <div class="stat-label">Clientes Satisfeitos</div>
  </div>
  <div class="stat-item">
    <div class="stat-num" data-target="640">0</div>
    <div class="stat-label">Figurinhas no Álbum</div>
  </div>
  <div class="stat-item">
    <div class="stat-num" data-target="48">0</div>
    <div class="stat-label">Seleções</div>
  </div>
  <div class="stat-item">
    <div class="stat-num" data-target="99">0</div>
    <div class="stat-label">% Satisfação</div>
  </div>
</div>

<!-- PRODUCTS GRID -->
<section>
  <div class="section-label reveal">Nossa Coleção</div>
  <h2 class="section-title reveal">PRODUTOS</h2>
  <div class="products-grid reveal">
    <div class="product-card">
      <span class="product-icon">📖</span>
      <div class="product-name">Álbum Capa Dura</div>
      <p class="product-desc">Edição especial com capa em relevo dourado. Perfeita para colecionar e guardar para sempre.</p>
      <div class="product-price">R$ 49,90 <small>/ unidade</small></div>
    </div>
    <div class="product-card">
      <span class="product-icon">⭐</span>
      <div class="product-name">Envelope de Figurinhas</div>
      <p class="product-desc">5 figurinhas por envelope. Distribuição aleatória com chance de figurinhas brilhantes e especiais.</p>
      <div class="product-price">R$ 4,50 <small>/ envelope</small></div>
    </div>
    <div class="product-card">
      <span class="product-icon">📦</span>
      <div class="product-name">Caixa com 50 Envelopes</div>
      <p class="product-desc">250 figurinhas em uma única caixa. A forma mais econômica de completar o álbum rapidinho.</p>
      <div class="product-price">R$ 199,90 <small>/ caixa</small></div>
    </div>
    <div class="product-card">
      <span class="product-icon">🏆</span>
      <div class="product-name">Kit Completo Starter</div>
      <p class="product-desc">Álbum + 10 envelopes para começar sua jornada rumo ao álbum completo.</p>
      <div class="product-price">R$ 69,90 <small>/ kit</small></div>
    </div>
    <div class="product-card">
      <span class="product-icon">✨</span>
      <div class="product-name">Figurinhas Avulsas</div>
      <p class="product-desc">Monte sua lista de figurinhas faltantes e garanta a sua completando seu álbum.</p>
      <div class="product-price">R$ 2,00 <small>/ figurinha</small></div>
    </div>
    <div class="product-card">
      <span class="product-icon">🎁</span>
      <div class="product-name">Kit Presente</div>
      <p class="product-desc">Álbum + 20 envelopes + embalagem especial. O presente ideal para o fã de futebol.</p>
      <div class="product-price">R$ 119,90 <small>/ kit</small></div>
    </div>
  </div>
</section>

<!-- PACKS -->
<section class="packs" id="pacotes">
  <div class="section-label reveal">Monte seu pacote</div>
  <h2 class="section-title reveal">PACOTES</h2>
  <div class="packs-grid reveal">
    <div class="pack-card highlight">
      <div class="pack-label">Mais Popular</div>
      <div class="pack-name">PACOTE CAMPEÃO</div>
      <p class="pack-desc">Álbum capa dura + 50 envelopes + 20 figurinhas avulsas à sua escolha. Tudo que você precisa para uma largada incrível.</p>
      <div class="pack-price">R$ 239</div>
      <button class="pack-buy">Comprar Agora</button>
    </div>
    <div class="pack-card">
      <div class="pack-label">Básico</div>
      <div class="pack-name">PACOTE TORCEDOR</div>
      <p class="pack-desc">Álbum + 10 envelopes para começar.</p>
      <div class="pack-price">R$ 69</div>
      <button class="pack-buy">Comprar</button>
    </div>
    <div class="pack-card">
      <div class="pack-label">Premium</div>
      <div class="pack-name">PACOTE LENDA</div>
      <p class="pack-desc">Álbum + 100 envelopes + 50 avulsas + brinde exclusivo.</p>
      <div class="pack-price">R$ 429</div>
      <button class="pack-buy">Comprar</button>
    </div>
  </div>
</section>

<!-- NEWSLETTER -->
<section class="newsletter" id="sobre">
  <div class="section-label reveal" style="position:relative;z-index:1;">Fique por dentro</div>
  <h2 class="section-title reveal" style="position:relative;z-index:1;">NOVIDADES<br>EM PRIMEIRA MÃO</h2>
  <p>Cadastre-se e receba lançamentos, promoções e dicas para completar seu álbum.</p>
  <div class="newsletter-form">
    <input type="email" placeholder="seu@email.com">
    <button>Cadastrar</button>
  </div>
</section>

<!-- FOOTER -->
<footer>
  <a href="#" class="footer-logo">COPA STORE</a>
  <ul class="footer-links">
    <li><a href="#">Produtos</a></li>
    <li><a href="#">Pacotes</a></li>
    <li><a href="#">Troca de Figurinhas</a></li>
    <li><a href="#">Contato</a></li>
    <li><a href="#">Instagram</a></li>
  </ul>
  <span class="footer-copy">© 2026 Copa Store</span>
</footer>

<script>
  // Custom cursor
  const cursor = document.getElementById('cursor');
  const ring = document.getElementById('cursorRing');
  let mx = 0, my = 0, rx = 0, ry = 0;
  document.addEventListener('mousemove', e => { mx = e.clientX; my = e.clientY; });
  function animateCursor() {
    cursor.style.left = mx + 'px'; cursor.style.top = my + 'px';
    rx += (mx - rx) * 0.12; ry += (my - ry) * 0.12;
    ring.style.left = rx + 'px'; ring.style.top = ry + 'px';
    requestAnimationFrame(animateCursor);
  }
  animateCursor();
  document.querySelectorAll('button, a, .product-card, .pack-card').forEach(el => {
    el.addEventListener('mouseenter', () => {
      cursor.style.width = '16px'; cursor.style.height = '16px';
      ring.style.width = '56px'; ring.style.height = '56px';
    });
    el.addEventListener('mouseleave', () => {
      cursor.style.width = '10px'; cursor.style.height = '10px';
      ring.style.width = '36px'; ring.style.height = '36px';
    });
  });

  // Scroll reveal
  const observer = new IntersectionObserver(entries => {
    entries.forEach((e, i) => {
      if (e.isIntersecting) {
        setTimeout(() => e.target.classList.add('visible'), i * 80);
      }
    });
  }, { threshold: 0.12 });
  document.querySelectorAll('.reveal').forEach(el => observer.observe(el));

  // Counter animation
  function animateCounter(el) {
    const target = +el.dataset.target;
    const duration = 2000;
    const start = performance.now();
    function update(now) {
      const progress = Math.min((now - start) / duration, 1);
      const ease = 1 - Math.pow(1 - progress, 3);
      el.textContent = Math.floor(ease * target).toLocaleString('pt-BR');
      if (progress < 1) requestAnimationFrame(update);
      else el.textContent = target.toLocaleString('pt-BR') + (el.dataset.target === '99' ? '%' : '+');
    }
    requestAnimationFrame(update);
  }
  const counterObs = new IntersectionObserver(entries => {
    entries.forEach(e => {
      if (e.isIntersecting) {
        e.target.querySelectorAll('[data-target]').forEach(animateCounter);
        counterObs.unobserve(e.target);
      }
    });
  }, { threshold: 0.5 });
  document.querySelector('.stats') && counterObs.observe(document.querySelector('.stats'));

  // Smooth scroll
  document.querySelectorAll('a[href^="#"]').forEach(a => {
    a.addEventListener('click', e => {
      const target = document.querySelector(a.getAttribute('href'));
      if (target) { e.preventDefault(); target.scrollIntoView({ behavior: 'smooth' }); }
    });
  });
</script>
</body>
</html>